# Lesson 10 terminology QA — id-ID

The complete 540-segment translation uses the cumulative component glossary.
Lesson 10 adds stable decisions O006-TERM-0143 through O006-TERM-0150 for
`fungsi kuasa`, `ukuran sampel`, `uji Wald`, `kerangka sampel`, `uji eksak`,
`uji konservatif`, `nilai ambang`, and `pelaksana jajak pendapat`.

Existing decisions remain controlling: `kuasa uji`, `tingkat signifikansi`,
`nilai-p`, `galat tipe I/II`, `daerah penolakan`, `informasi Fisher`,
`informasi teramati`, and `gagal menolak hipotesis nol`. The target does not
translate rejection as proof, does not call failure to reject acceptance, and
does not present Wald or Normal approximations as exact finite-sample results.

Symbols and executable identifiers—including alpha, beta, mu, p, c, Z,
`optim`, and `out$hessian`—remain unchanged. `bushel` is retained as the unit
name and `acre` is rendered as `ekar`. The source's 28 proved mathematical,
mechanical, topology, accessibility, and reproducibility findings are handled
only in the derivative correction layer; authority bytes remain immutable.
