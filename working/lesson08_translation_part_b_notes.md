# Lesson 08 translation part B

- Scope: exactly `O006-PSU-009-S0101` through `O006-PSU-009-S0200` (100 ordered segments).
- Terminology follows the cumulative id-ID glossary and the Lesson 08 terminology QA: `penduga`/`nilai dugaan`, `fungsi kemungkinan`, `bootstrap parametrik`, `bootstrap nonparametrik`, `pengambilan sampel ulang (resampling)`, `distribusi empiris`, `kuantil`, and `selang kepercayaan`.
- Registered corrections applied in this prose slice: bootstrap results are called `nilai dugaan bootstrap`, not data samples (`L08-D001`); the empirical-distribution explanation counts duplicate multiplicity (`L08-D003`); and the sentence-initial capitalization plus lettered template instructions are repaired (`L08-D017`).
- Executable identifiers and inline code nodes are outside the translation map and remain untouched. In particular, the registered source tokens `model.c.` and `opti` still require the later correction layer specified by `L08-D017`.
- Stale numerical-output chains were not normalized or guessed; their seeded reconstruction remains assigned to the later correction/build layer (`L08-D007`–`L08-D008`).
- Validation: exact sorted key coverage, nonempty string values, and no U+FFFD replacement characters.
