# Durable task and finite workflow — O006/C140 STAT 415 component

## Objective and authority

Produce the complete Bahasa Indonesia `id-ID` edition of the official Penn
State STAT 415 public sequence as the external narrative spine for O006/C140.
The exact external boundary is the official landing/index plus Lessons 00–12,
fourteen documents in source order. This is a production task: natural reader
translation and a usable self-study course dominate. Audit and QA are bounded
gates, not substitutes for translation. The independent 29-page Random edition
is finished in `../mathematical-statistics-id` and must never be overwritten or
folded into this repository.

Authority comes only from official Penn State responses, the exact expected
manifest under `authority/expected/`, and the component architecture copied in
`00_control/COMPONENT_BOUNDARY.md`. Freeze response bytes, final URLs, response
headers, SHA-256, anchors, direct assets, code, figures, and licence witnesses.
The public distribution is semantic Quarto-generated HTML; do not invent an
upstream Git repository, QMD source, configuration, tag, commit, or exact
Quarto-source reproduction. Reconstruct a normalized semantic authoring layer
and an explicit source-URL/anchor map. Preserve CC BY-NC 4.0 and every
component exception; link, replace, or omit uncleared assets rather than
assuming the page notice covers them.

## Sequential production

1. Verify the fourteen expected document identities and retrieve them once
   into immutable `authority/upstream/stat415/`. Record a deterministic URL and
   header manifest plus a freeze receipt. Never mutate frozen bytes.
2. Inventory every main-content heading, anchor, theorem/definition/example,
   worked solution, formula, code block, figure, dataset/app link, and direct
   asset. Freeze only rights-cleared dependencies. Record excluded or linked
   components and reasons.
3. Establish locale-neutral stable IDs for documents, sections, structural
   units, math, examples, solutions, code, assets, rights, corrections,
   translation segments, builds, QA, and artifacts. IDs cannot contain
   translated wording. Bind order, hierarchy, upstream URL/anchor, source and
   target hashes, locale state, rights state, and prerequisite/objective tags
   in canonical UTF-8 JSONL/CSV/JSON.
4. Translate contiguously: landing/index plus all Lesson 00 first, then Lessons
   01–12 in source order. Preserve mathematics, identifiers, topology,
   objectives, proofs, examples, all 76 worked-solution surfaces, code,
   figures, navigation, attribution, and component licences. Lesson 00 and
   elementary R remain present but may be marked diagnostic/optional. Repair
   only independently proved defects in the target and record each in an
   adverse ledger; specifically do not repeat Lesson 07's unsupported claim
   that consistency alone implies convergence of expectations.
5. Build deterministic offline HTML after each substantial contiguous batch.
   At the complete component boundary also build PDF and EPUB. Use ordinary
   links where possible, local open runtimes, accessible navigation and
   disclosures, useful alt text/static equivalents, and no unresolved local or
   credential-bearing path.
6. Run bounded structural, formula, ID, link, asset, rights, privacy,
   accessibility, language, desktop/mobile, and deterministic replay checks.
   Hard-fail missing or extra main content, formulas, anchors, examples,
   solutions, code, and figures. Fix material findings once, record exact
   counts/hashes, then advance; do not loop on QA.
7. The separate original companion closes rather than disguises source gaps:
   regular-likelihood conditions/proofs, optimality, matrix simple/multiple
   regression, reproducible simulations, thirteen mastery sets, four
   cumulative assessments, and two capstones. The exact Random completeness
   donor remains a distinct licensed component. Never present companion prose
   as Penn State prose.
8. At each substantial passing boundary, update the durable cursor/state,
   commit and push the dedicated public repository without asking for renewed
   confirmation, then anonymously verify every public manifest byte and live
   reader route. Publish worthwhile releases to the correct preservation
   lineage with reader-first files and anonymous readback. Do not create empty
   or duplicate records.

## Durability, boundaries, and terminal condition

After any context loss, ignore conversation summaries. Read this file,
`CURRENT_CURSOR.md`, `CURRENT_STATE.md`, `COMPONENT_BOUNDARY.md`, the authority
manifest/receipt, final ledger rows, backend receipt, and latest build/QA and
publication receipts. Update them at every material decision and contiguous
checkpoint with exact paths, counts, hashes, unresolved work, and the next
executable action. Never run broad workspace or Git scans and never touch
another lane.

Do not contact Penn State or other authors during production. After the entire
relevant corpus, at most one concise, deduplicated, high-confidence issue may
be sent if separately authorized, signed `Codex — at the user’s direction`.
This component is complete only when all fourteen documents are translated and
bound, assets/rights are closed, HTML/PDF/EPUB and backend deterministically
pass, public bytes are released/read back, and the required companion/donor
collection boundaries are explicit. Overall O006/C140 remains active until the
donor, original companion, notebooks, mastery corpus, assessments, capstones,
and all component-preserving releases are also complete.
