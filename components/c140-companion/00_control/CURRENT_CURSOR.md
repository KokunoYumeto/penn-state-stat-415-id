# Production cursor — original C140 companion

Updated: 2026-08-28

Current boundary: C1 local reader/backend/simulation/QA complete. Exact admitted
scope is `D001`–`D007`, `SIM001`–`SIM004`, `MS07`–`MS10`, and `CA01` under
`source/id-ID/`, plus generated simulations, the 35-file HTML reader, and the
469-entity/648-relation backend.

Next executable action: package the coherent C1 checkpoint while preserving
every one of the 25 prior release files byte-for-byte; replay CI; commit/push to
the existing repository; wait for static Pages; anonymously verify all 159
public files; publish the cumulative package in the existing Zenodo concept
and anonymously verify every file and zero drafts. Then begin the matrix
Gaussian linear-model batch without changing component ownership or paths.

Recovery: read `WORKFLOW.md`, `CONTENT_CONTRACT.md`, this cursor,
`CURRENT_STATE.md`, `CHECKPOINT_2026-08-28_C1_LOCAL_COMPLETE.md`, README/LICENSE,
the exact C1 sources, `build/C1_*_RECEIPT.json`, backend manifests, latest
package/publication receipts, and repository-level state/cursor. Conversation
summaries are not state. Do not broaden filesystem or Git scope.
